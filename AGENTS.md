## Communication

- Be simple and concise.
- Use the active voice.
- Use markdown code blocks (```) instead of Markdown blockquotes in responses.

## Git and GitHub

- Push when the session's main task is complete or the user asks to push.
- If the project has no remote, create a private GitHub repository when it is
first pushed.
